import { answer } from './{{name}}';

describe('answer', () => {
    test('test1', () => {
        expect(answer("arg")).toBe(true);
    });

    test('test2', () => {
        expect(answer("arg")).toBe(false);
    });

    test('test3', () => {
        expect(answer("arg")).toBe(true);
    });
});